EXPECTED_EMAIL="emlopezdev@gmail.com"
ACTUAL_EMAIL=$(git config user.email)

if [ "$ACTUAL_EMAIL" != "$EXPECTED_EMAIL" ]; then
  echo ""
  echo "  ✗ Wrong git email: $ACTUAL_EMAIL"
  echo "    Expected:         $EXPECTED_EMAIL"
  echo ""
  echo "  Fix: git config --global user.email '$EXPECTED_EMAIL'"
  echo ""
  exit 1
fi

npm run lint
npm run typecheck
